[pytest]
testpaths = tests
python_files = test_*.py
addopts = --tb=short -ra --strict-markers
markers =
    smoke: quick smoke test
    integration: integration test requiring seeded data
    security: security/edge case test
